---
title: Static Hydration
slug: static-hydration
intro: "A topic for the build pattern: static HTML first, React hydration after."
---

# Static Hydration

The production build prerenders each route into a plain HTML file.

Once the browser loads the JavaScript bundle, React hydrates the same markup and keeps the page interactive.
